# Auto detect text files and perform LF normalization
* text=auto

# Precomputed data files (shipped with repo, binary parquet)
data/*.parquet binary
data/*.parquet filter=lfs diff=lfs merge=lfs -text
