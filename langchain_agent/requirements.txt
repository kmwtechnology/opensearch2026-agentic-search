langchain-core>=1.2.5,<2.0.0
langchain-google-genai>=4.0.0,<5.0.0
langgraph>=1.0.5,<2.0.0
langgraph-checkpoint-postgres>=3.0.2,<4.0.0
psycopg[binary]>=3.3.0
psycopg-pool>=3.3.0
opensearch-py>=2.4.0

# Web API dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=14.0
httpx>=0.28.0
pydantic>=2.10.0

# Security and rate limiting
slowapi>=0.1.9
python-dotenv>=1.0.0
# Required by Starlette SessionMiddleware (signed cookie used by login gate).
itsdangerous>=2.2.0

# Retry logic
tenacity>=8.2.0

# Structured logging
structlog>=24.0.0

# Data processing (for ESCI product ingestion)
pandas>=2.0.0
pyarrow>=10.0.0

# Cross-encoder reranker (fast local relevance scoring)
sentence-transformers>=3.0.0

# LangSmith observability (optional - for tracing and debugging)
langsmith>=0.1.0
